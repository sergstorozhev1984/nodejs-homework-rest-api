const createProject = require("./createProject");
const getAll = require("./getProjectsList");
const getFilteredProjects = require("./getFilteredProjects");

module.exports = {
  createProject,
  getAll,
  getFilteredProjects,
};
